import 'package:flutter/material.dart';

class JobHomeFab extends StatelessWidget {
  const JobHomeFab({super.key});

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {
        // TODO
      },
      child: const Icon(Icons.more_horiz),
    );
  }
}
